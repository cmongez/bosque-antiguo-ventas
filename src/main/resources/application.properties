spring.application.name=ventas
# Configuración del servidor
server.port=8082

# Configuración de la Base de Datos (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/ventas?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA/Hibernate
# create-drop: borra y crea la BD cada vez (bueno para desarrollo)
# update: actualiza el esquema (bueno para desarrollo)
# validate: valida que el esquema coincida (bueno para producción)
# none: no hace nada
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect